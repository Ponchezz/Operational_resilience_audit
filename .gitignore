# R / RStudio
.Rproj.user
.Rhistory
.RData
.Ruserdata
*.Rproj

# renv (if you use it)
renv/
renv.lock

# Python (if any)
__pycache__/
.ipynb_checkpoints/

# Data & outputs (keep local, not in git)
data/*.*        # keep the folder, not the files
!data/.gitkeep  # allow placeholder
output/
outputs/

# Big binary artifacts (keep in Releases or use Git LFS)
*.png
*.pdf
*.html
*.pptx
*.docx

# OS noise
.DS_Store
Thumbs.db

# Secrets
kaggle.json
