# R / RStudio
.Rproj.user
.Rhistory
.RData
.Ruserdata
*.Rproj

# renv (if you use it)
renv/
renv.lock

# Python (if any)
__pycache__/
.ipynb_checkpoints/

# Data & outputs (keep local, not in git)
data/*.*        # keep the folder, not the raw files
!data/.gitkeep  # allow a placeholder
output/
outputs/

# Big binary artifacts (keep in Releases or LFS)
*.pptx
*.docx
*.pdf
*.html
*.png

# OS noise / secrets
.DS_Store
Thumbs.db
kaggle.json
